apiVersion: v1
kind: Service
metadata:
  name: {{ include "model-core-base-image.fullname" . }}
  labels:
    {{- include "model-core-base-image.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "model-core-base-image.selectorLabels" . | nindent 4 }}
