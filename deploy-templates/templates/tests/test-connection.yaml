apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "model-core-base-image.fullname" . }}-test-connection"
  labels:
    {{- include "model-core-base-image.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "model-core-base-image.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
